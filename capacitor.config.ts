import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.maharshi.notes',
  appName: 'note',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
